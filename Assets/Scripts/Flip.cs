using UnityEngine;

public class Flip : MonoBehaviour
{
    /*public float _horiz = Player._horizontal;
    private bool _flipRight = true;

    private void Update()
    {
        if ((!_flipRight && _horiz > 0) || (_flipRight && _horiz < 0))
        {
            transform.localScale *= new Vector2(-1, 1);
            _flipRight = !_flipRight;
        }
    }
*/
}
